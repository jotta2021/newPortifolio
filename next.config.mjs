/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['camo.githubusercontent.com', 'raw.githubusercontent.com', 'user-images.githubusercontent.com']
    }
};

export default nextConfig;
